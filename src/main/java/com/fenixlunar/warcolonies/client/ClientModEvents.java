package com.fenixlunar.warcolonies.client;

import net.neoforged.api.distmarker.Dist;
import net.neoforged.fml.common.EventBusSubscriber;

/**
 * MVP: sem registrar telas antigas aqui.
 * (Vamos registrar a tela do arquiteto corretamente depois, no lugar certo do seu fluxo atual.)
 */
@EventBusSubscriber(modid = "warcolonies", value = Dist.CLIENT)
public class ClientModEvents
{
}
